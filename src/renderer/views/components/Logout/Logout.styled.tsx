import styled from "styled-components";

export default styled.div`
  position: relative;

  button {
    background-color: transparent;
    border: none;
    color: white;
    position: fixed;
    right: 4px;
    top: 8px;
  }
`;
